@model Basketbool.Web.Models.MatchDayDetailViewModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Team in group: @Model.MatchDay.Name</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditMatchDayDetail" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="MatchDayId" />
            <input type="hidden" asp-for="Id" />

            <partial name="_MatchDayDetail" />

            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a asp-action="DetailsGroup" asp-route-id="@Model.MatchDayId" class="btn btn-success">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
