@model Basketbool.Web.Models.MatchDayViewModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>MatchDay to Season: @Model.Season.Name</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddMatchDay" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SeasonId" />

            <partial name="_MatchDay" />

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a asp-action="Details" asp-route-id="@Model.SeasonId" class="btn btn-success">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
